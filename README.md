# node-basic-table
My first steps in Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     It is the base of the multiplication table  [number] [required]
  -l, --list     Displays the table in the console    [boolean] [default: false]
  -t, --to       Set what will be the limit number.       [number] [default: 10]
```
